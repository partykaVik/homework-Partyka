function getMaxDigit(n){
    let maxDigit = +Math.max(...(n + ``).split(``));
    return maxDigit
    }
    console.log(getMaxDigit())


function getNormalCase(`name`){
    let name = (`name`).toUpperCase()
    return name
}
console.log(getMaxDigit())